# 2026-06-23

## 编译打包 v1.7.0 安装包
- 版本号 1.6.0 → 1.7.0，更新 3 个文件：`main.py`、`workload.spec`、`build_installer.iss`
- PyInstaller 打包成功 → `dist\工作量统计工具_v1.7.0\`
- Inno Setup 编译成功 → `installer\工作量统计工具_v1.7.0_Setup.exe`

## 版本号同步
- `Layout.tsx` 关于对话框: v1.6.0 → v1.7.0
- `tray_app.py` 托盘标题: v1.6.0 → v1.7.0
- `build_installer.iss` 注释: v1.1.0 → v1.7.0

## 统计页圆角优化
- 最终: 全部页面/组件统一 `borderRadius: '2px'` 小r
- 涉及文件: StatsPage, StatsCards, ManagePage, EntryPage, HomePage, ProjectRow, Layout, GroupCard, App (共 55+ 处)
- 保留: main.tsx 主题配置, '50%' 圆形元素

## Git 仓库
- 创建 `.gitignore`，关联远程 `origin → https://github.com/HotLL233/tj`
- 首次提交并推送成功 (v1.7.0)

## 导出模板重构 + v0.2.1 打包
- 分析 `导出模板\汇总模板.xlsx`（月-汇总 + 周-汇总两个Sheet）
- 创建 `export_v3.py`：对齐模板格式（F列宽19.66、删除FONT_R/FONT_B、周汇总F-J合并表头+K/L/M汇总列、无小计行、用户名列合并）
- 修复列宽覆盖顺序bug（_auto_fit先执行，WIDTHS后覆盖）
- `__init__.py` 切换路由：`export` → `export_v3`
- 版本号 v0.2.0 → v0.2.1：main.py、tray_app.py、Layout.tsx、build_installer.iss、workload.spec
- 前端 build + PyInstaller + Inno Setup 编译全部成功
- 安装包：`installer\工作量统计工具_v0.2.1_Setup.exe` (44.5MB)
