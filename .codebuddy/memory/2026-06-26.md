# 2026-06-26

## v0.1.5: Phase1架构加固（配置化+审计+Service层）
- 按 `Rust架构改进实施方案.md` Phase 1 执行
- **配置化** `src/config.rs`：AppConfig 从 config.toml 加载（端口/DB路径/日志级别），不存在则用默认值
- **审计抽象** `src/audit.rs`：`execute_with_audit` 统一事务+审计日志写入，消除 repo 中手动 log_on_conn 重复
- **Service 层** `src/service/record_service.rs`：Handler 只做参数提取和 HTTP 响应，业务校验和编排下沉到 Service
- **日志系统** `tracing + tracing-subscriber + tracing-appender`：生产模式写文件日志，开发模式控制台
- 新增依赖：toml、tracing-appender
- main.rs 重构：配置驱动端口/DB路径/tracing 初始化
- record_handler 重构：create/update/delete 委托 service 层

## v0.1.4: 导出5Sheet完整实现
- 从 v0.1.3 复制到 v0.1.4，严格版本隔离
- **全部5个Sheet完整实现**（对齐Python export_v4.py）：
  - Sheet1 月-汇总：lab→code→instrument 三级树形+B-I模板+SUM公式+总计行
  - Sheet2 每日工作量：date→lab/code→instrument 四级树形+B-I模板+合并+公式
  - Sheet3 每周工作量：月汇总基础上+周数据列(J起)+周范围计算(chrono Datelike)
  - Sheet4 原始记录：序号/日期/实验室/项目/仪器/方法/类型/数量/录入人 9列
  - Sheet5 用户统计：user→lab/code→instrument 四级树形+B-I模板
- 公共函数：`detect_groups`/`write_tree_sheet` 供Sheet1/2/5复用
- 工具函数：`week_ranges`(chrono Datelike)、`build_lab_hierarchy`、`flatten_lab`

## Python 版冻结
- v0.2.5 为最终版，不再更新
- 版本号同步：main.py/tray_app.py/api/__init__.py/workload.spec → v0.2.5

## Rust 版开发策略更新
- 版本隔离原则：每次迭代新建版本号文件夹，不覆盖旧代码
- 基线版本 v0.1.3 已快照到 `workload-tool-rust/v0.1.3/`（29个文件完整副本）
- 下次迭代：复制 v0.1.3 → v0.1.4，在新文件夹内修改
- v0.1.3 主要修复：导出 Excel 文件损坏（_cl 0-based、merge先合后写、公式跳过覆盖行、总计行修正 F/G/H/I）
- 打包流程标准化写入 MEMORY.md
