# v0.4.82 更新说明

发布日期：2026-07-16

本次迭代在独立目录 `v0.4.82` 内完成。创建新版本目录时未从旧版本复制 `target`、`node_modules`、`dist`、`installer` 等构建产物；`backend/static` 也未带入旧版前端构建文件，仅在本版本重新生成。

## 本次修改

1. 记录表格改为页面内完整显示
- 研发送样记录二级页面：取消按内容累计宽度的横向长表，改为 `100%` 页面宽度内按比例分配列宽。
- 统计页面检测人记录：取消 `max-content` 和大 `minWidth`，所有列在页面内按百分比分配。
- 分析检测录入页今日记录、研发送样录入页今日记录：同步改为页面宽度内固定布局。
- 日期时间、方法、项目、高项、仪器等长字段全部允许自动换行，保证内容不折叠、不省略。
- 表格字体、表头、单元格 padding 调整为紧凑显示，减少短字段占用空间。

2. 版本与安装包
- 应用版本升级为 `0.4.82`。
- 前端版本同步为 `0.4.82`。
- 安装包输出文件名更新为 `样品管理系统_v0.4.82_Setup.exe`。
- 安装包继续沿用相同 `AppId` 和安装目录，可覆盖上一个安装版本。

## 验证结果

- `cargo check`：通过
- `npm run build`：通过
- `cargo test`：通过
- `cargo build --release`：通过
- Inno Setup 安装包编译：通过
