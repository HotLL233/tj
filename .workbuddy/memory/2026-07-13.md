## v0.4.47 (2026-07-13 10:24-10:48) — 用户撤回
## v0.4.48 (2026-07-13 10:52-11:05) — 修复 v0.4.47 错误
## v0.4.49 (2026-07-13 11:22-11:38) — 方案B：统一录入表单配置器
## v0.4.52 (2026-07-13 14:09-14:20) — 列宽修复 + 阶段C安全修复

### 列宽修复（P0）
- `minWidth: 1280` + `minWidth: field.width || 80` → 表宽强制溢出 → 列被切
- 修复：`tableLayout: 'fixed'` + `width: '100%'`；移除 minWidth；表头checkbox加width

### 阶段C（安全）
- JWT_SECRET：硬编码const → `std::env::var("JWT_SECRET")`运行时读取
- SQL注入：audit_repo.rs 移除 format!拼接 → 白名单(work/rd/shared)校验
- 密码：config.rs 新增 `ADMIN_PASSWORD` 环境变量支持
- 清理：删 frontend/src/src/ 嵌套目录 + .bak文件

### 推送
- workload-tool: ec92f9c + tag v0.4.52

### 修复
- 录入表复选框/序号cell无width → 与表头%布局不匹配 → 最后一列被切
- 4处加 `tableConfig.seq_column_width` / `checkbox_column_width`

### 新增
- ManageExportConfig.tsx（330行）：文件名模板、Sheet开关/名称/颜色、列宽/标签
- ManagePage 加 `exports` tab
- export_handler.rs 加 `apply_sheet_cfg()`：导出时读取模板覆盖名称/颜色/列宽
- 种子 export_template_workload/rd/sample_info

### 推送
- workload-tool: 9a21bda + tag v0.4.51
- Docker CI 自动触发

### 新增
- `TableConfig`：row_height / seq_column_width / checkbox_column_width
- `FormLayout` 兼容格式（`{table_config, fields}`）
- ManageFormConfig 加「表格全局设置」编辑（3 个数值输入框）
- 预览表格按配置渲染（序号列、复选框列、行高）

### 向后兼容
- 读 v0.4.49 纯 `FieldDef[]` 数组 → 使用 DEFAULT_TABLE_CONFIG
- 读 v0.4.50 `FormLayout` → 带 table_config 覆盖

### 种子
- DB 种子 3 条 form_* 改为 FormLayout 格式

### 推送
- workload-tool: 45c0318 + tag v0.4.50
- Docker CI 自动触发

### 发现根因（深入分析结论）
- PageLayoutAdmin 保存到 `sample_entry_fields`，SampleEntryPage 读取 `layout_sample_entry_fields` —— **key 不一致**
- DB 种子用的是 `layout_sample_entry_fields`
- 这就是"保存成功但不生效"的真正原因
- SampleInfoEntry 用独立 `SampleInfoColumn` 系统，从未接入 FieldDef
- EntryPage 硬编码表单，也没有 FieldDef

### 方案B实施
1. **ManageFormConfig.tsx（新增 300 行）**：
   - 3 个表单模板：研发送样 / 样品信息 / 分析检测
   - 字段编辑器：拖拽排序、标签、宽度、类型、可见开关、删除、添加
   - 实时预览表格（所见即所得）
   - 存储 key：`form_sample_entry` / `form_sample_info_entry` / `form_entry`

2. **ManagePage**：加 `forms` tab，图标复用 ScienceIcon

3. **PageLayoutAdmin key 修复**：`sample_entry_fields` → `layout_sample_entry_fields`（兼容旧版）

4. **SampleEntryPage**：读取 key 改为 `form_sample_entry`（回退兼容 `layout_sample_entry_fields`）

5. **SampleInfoEntry**：
   - 加 `formDefs` 状态（从 `form_sample_info_entry` 加载）
   - `useMemo` 合并 FieldDef → SampleInfoColumn（覆盖 width/label/visible）
   - formColumns/listColumns 改用 mergedColumns

6. **DB 种子**：新增 `form_sample_entry` / `form_sample_info_entry` / `form_entry` 3 条

7. **类型扩展**：`FieldDef.type` 加 `datetime`（LayoutDesigner 同步更新）

### 验证
- ✅ tsc 0 errors
- ✅ vite build 2.83s
- ✅ cargo build 1m41s
- ✅ curl PUT form_sample_entry → 200 OK + GET 验证 → 正确
- ✅ form_sample_info_entry 种子数据 9 fields
- ✅ installer 打包成功
- ✅ workload-tool push 成功（0847592 + tag v0.4.49）

### 文件清单
- 新增：ManageFormConfig.tsx
- 修改：ManagePage.tsx, SampleEntryPage.tsx, SampleInfoEntry.tsx, PageLayoutAdmin.tsx, LayoutDesigner.tsx, layout.ts, migrations.rs
- 总计 8 个文件

### 错误
- v0.4.47 在 `Layout` 加了全局 `PageEditProvider` + `SampleEntryPage` 加 `PageEditToggle` 按钮
- **违反了 v0.4.44 的设计**（编辑功能全部转移至管理页集中入口，外部不显示）
- 推到 tj 仓库（tj 是个人工作仓库，不应推工具代码）
- 复制时带入了 v0.4.22-v0.4.47 的历史 CHANGELOG/设计文档

### 撤销
- tj 仓库强制回退到 e372888 (v0.4.46)，删除 v0.4.47 tag

## v0.4.48 (2026-07-13 10:52-11:05) — 修复版

### 用户反馈
- "你动我旧版本的文件干什么？为什么还推送到tj仓库？不是所有编辑页面入口全部转移到统一的管理入口内吗？"

### 真正修复（保留的 5 个）
- `settings_handler.rs` UTF-8 安全切片（panic 修复）
- `SampleEntryPage` 补 `lab_name` 分支
- `SampleEntryPage` 9 cell 改 100% 弹性布局
- `frontend/public/favicon.ico` + index.html link
- ISS 路径 v0.4.48

### 回退（v0.4.47 错误添加）
- ❌ `Layout` 全局 PageEditProvider 包裹 → 删除
- ❌ `SampleEntryPage` PageEditToggle 按钮 → 删除
- ✅ 编辑功能只在管理页（v0.4.44 设计）

### 版本隔离清理
- 删除 v0.4.22-v0.4.47 全部历史 CHANGELOG
- 删除 arch-design / design-*.md 历史设计文件
- 删除 docs/ 目录（class-diagram/sequence-diagram/system_design）
- 删除 dist/ static/ backend/ 构建产物（让 build 重新生成）
- 删除 frontend/vite.config.ts.timestamp-*.mjs / StatsPage.tsx.bak 残留

### 推送
- ✅ workload-tool 仓库（force-push，v0.4.47 tag 删除，v0.4.48 tag 新建）
- ❌ **不再推 tj 仓库**

### 重要教训（CRITICAL）
- **tj vs workload-tool 仓库分离**：
  - `HotLL233/tj` = 个人工作跟踪（自己的工作量、规划、笔记）— 工具代码**绝不能推到这里**
  - `HotLL233/workload-tool` = 工具代码仓库 — 工具版本推到这里
  - 默认只推 workload-tool，除非用户明确要求推 tj
- **管理页集中式编辑设计**（v0.4.44 铁律）：
  - 编辑功能**只能**在管理页 → 页面布局管理 tab
  - 外部 12 个页面**绝对不能**加 PageEditToggle/PageEditProvider
  - 恢复内联入口 = 推翻 v0.4.44 设计 = 重新设计，需用户明确同意
- **版本隔离复制时**（每次必做清理）：
  - `rm -f CHANGELOG_v0.4.[2-9]*.md` 旧版本变更日志
  - `rm -f design-*.md arch-design-*.md` 设计文档
  - `rm -rf docs dist static backend` 构建产物
  - `rm -f frontend/vite.config.ts.timestamp-*.mjs` vite 临时文件
  - `rm -f *.bak` 备份文件
- **Cargo.toml 版本号陷阱**（v0.4.45 血的教训）：cp 后要 sed 三处
- v0.4.56 (15:56-16:14): RdRecordsPage编辑面板硬编码→columns动态渲染; sample_info_column_repo INNER JOIN→LEFT JOIN; 软删除文案+恢复按钮
- v0.4.57 (16:45-16:57): TruncatedCell长字符Tooltip组件; BackToTop回到顶部副组件; Layout全局BackToTop; 检测类型删除→回收站(仅显示启用的,永久删除API); RdRecordsPage TruncatedCell
