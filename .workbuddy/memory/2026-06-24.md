# 2026-06-24 工作日志

## Rust 工作量统计工具 — 路由 405/HTML Bug 根因修复

### 问题
- 所有带路径参数的 API 路由（`PUT /api/records/1`、`DELETE /api/records/1` 等）返回 HTML 而非 JSON
- 静态路由（`GET /api/records`、`GET /api/groups`）正常返回 JSON
- 请求落入 SPA fallback `serve_index`，返回 `index.html`

### 根因
**axum 0.7.9（matchit 0.7.3）使用 `:id` 语法，不是 `{id}` 语法**

- `{id}` 语法是 axum 0.8（matchit 0.8）才引入的
- 在 matchit 0.7.3 中，`{id}` 被当成字面量字符串，路由 `/api/records/{id}` 只匹配 URL `/api/records/{id}` 本身
- 所有带数字 ID 的请求（如 `/api/records/1`）无法匹配，落入 fallback

### 修复内容
1. **路径参数语法**：所有 handler 中 `{id}` → `:id`，`{user_name}` → `:user_name`
2. **路由架构**：`.nest()` → `.merge()` + 完整路径前缀（每个 handler 使用 `/api/xxx/:id` 而非相对路径）
3. **serve_index 编译错误**：`(String, StatusCode)` → `(StatusCode, String)` 修复 axum 0.7 IntoResponse trait bound
4. **SPA fallback 恢复**：`.fallback(serve_index)` 正确配置，读取 `static/index.html`

### 修改文件
- `src/api/mod.rs` — `.nest()` → `.merge()`
- `src/api/group_handler.rs` — `{id}` → `:id` + 完整路径
- `src/api/project_handler.rs` — `{id}` → `:id` + 完整路径
- `src/api/record_handler.rs` — `{id}` → `:id` + 完整路径 + 恢复 restore/by-user 路由
- `src/api/stats_handler.rs` — 完整路径前缀
- `src/api/export_handler.rs` — 完整路径前缀
- `src/api/audit_handler.rs` — 完整路径前缀
- `src/main.rs` — 修复 serve_index 编译错误 + 恢复 fallback

### 验证结果（全部通过）
- `PUT /api/records/1` → ✅ JSON（quantity 更新为 99）
- `DELETE /api/records/2` → ✅ JSON（删除成功）
- `POST /api/records/restore/2` → ✅ JSON（恢复成功）
- `PUT /api/groups/1` → ✅ 路由匹配（400 是 curl 中文编码问题）
- `PUT /api/projects/1` → ✅ JSON
- `GET /api/audit-logs` → ✅ JSON（含 update/delete/restore 审计日志）
- `POST /api/records` → ✅ JSON（新建 id=10）
- 前端 `client.ts` 无需修改（baseURL `/api` + 相对路径已正确）

### 之前已修复的 record_repo.rs 事务原子性
- `create`/`update`/`soft_delete`/`restore`/`delete_by_user` 全部用 `conn.transaction()` 包裹
- 审计日志统一用 `log_on_conn(&tx, ...)`
- `update` 新增 UPDATE 影响行数检查
- `restore` 新增记录存在性 + 是否已删除检查
