# 2026-06-30

## 方法导入模块重构 — v0.2.15

### 需求
用户 Excel 模板是"每列独立"结构：列头=分组名，列值=条目名。各列无交叉从属关系。

### 版本隔离
从 v0.2.14 复制 → v0.2.15，只改 2 个文件：
- `src/api/project_handler.rs` — 重写 `method_import` 函数（行模式→列模式）+ 新增 `classify_method_type` 辅助函数
- `src/repo/project_repo.rs` — 删除 `batch_import_3level`，替换为 `batch_import_by_column`

### 解析规则
- 列头含"方法" → method_type = "检测方法"
- 列头含"研发" → method_type = "研发项目"
- "实验室管理"列 → 只建分组，不建项目
- 其他 → 列头原值
- 去重：内存层 + DB 层双重保障

### QA 验证
- cargo check: ✅ 0 errors
- 6 项逻辑审查: ✅ 全部 PASS
- 未修改：前端、路由、DB schema

## v0.2.16 — 卡片合并 + 项目编辑多选关联

### 需求
A. 删除理化/ICP/热分析/其他方法4个独立卡片 → 全部合并到"检测方法管理"卡片，按group_name分组展示
B. 项目编辑对话框增加：关联实验室(多选) + 关联检测方法(级联多选)

### 修改文件 (5个)
**后端**：
- `migrations.rs` +2行 ALTER TABLE (associated_lab_ids, associated_method_ids)
- `models/project.rs` ProjectResponse/Create/Update 各+2字段
- `repo/project_repo.rs` PROJ_SQL +2列, row_to_project/create/update 适配

**前端**：
- `types/index.ts` Project接口 +2字段
- `ManagePage.tsx` TC -4项, TV -4, methods卡片重写~80行, 对话框+50行

### QA验证
- cargo check: ✅ 0 errors
- vite build: ✅ 通过
- 5项审查: ✅ 全部PASS
