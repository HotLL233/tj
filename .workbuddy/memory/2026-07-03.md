# 2026-07-03

## v0.3.6 编译错误修复 + 重新打包

1. **export_handler.rs**: 缺少 `use chrono::Datelike;`
2. **export_write.rs**: 11 处 `write_formula(format!(...))` 缺少 `.as_str()`
3. **export_write.rs L516**: 工作占比公式 CM(总额)→CN(平均值)
- `installer/工作量统计工具_Rust_v0.3.6_Setup.exe`（5.2MB）

## v0.3.7 导出模板对齐 + StatsPage 卡片重塑（标准 SOP 流程）

### SOP 流程
产品经理(PRD) → 架构师(系统设计) → 工程师(T01-T03) → 编译打包(T04)

### 关键修复
1. **export_write.rs Sheet 6/7/8 HashMap 聚合 Bug**：删除 HashMap，逐行遍历 rows。SQL 已正确 GROUP BY，HashMap 二次聚合导致系数/金额被覆盖
2. **v0.3.7 版本隔离**：仅复制 src/ + static/ + Cargo.toml + icon.ico + build_installer.iss（1.3MB vs 805MB）
3. **10 个预览 API**：`/api/export/preview/sheet1~10`，复用 export_data 查询函数
4. **StatsPage 12 张卡片**：2 概览(按周/按月) + 10 导出预览 Sheet，所见即所得

### 文件改动
- 新增：v0.3.7/全目录, export_preview_handler.rs, frontend/PreviewTable.tsx
- 修改：export_write.rs(3函数), export_data.rs(Serialize), api/mod.rs, types/index.ts, api/client.ts, StatsPage.tsx, StatsCards.tsx
- 安装包：`installer/工作量统计工具_Rust_v0.3.7_Setup.exe`

### 编译状态
## v0.3.7 method_id 核心修复 — work_records 增加方法关联

**问题**：work_records 只有 project_id 没有 method_id，统计/导出通过 project_method_links 反推方法，一个项目多方法导致一条记录拆成多条。

**修复**（8 个文件）：
1. migrations.rs: ALTER TABLE + 索引 + 历史数据回填
2. record.rs: RecordCreate 加 method_id, RecordResponse 加 method_id
3. record_repo.rs: INSERT/列表 SQL 改为 LEFT JOIN methods ON wr.method_id
4. record_handler.rs: 透传 method_id
5. export_data.rs: 10 Sheet 全部改用 wr.method_id 替代 project_method_links
6. EntryPage.tsx: 提交时传 method.id
7. client.ts: createRecord 参数加 method_id
8. StatsPage.tsx: method_name/type 从后端直接取

编译 ✅ vite ✅ tsc ✅ 安装包已更新

**问题**：预览端点返回 `Json<Vec<T>>`（裸数组），前端期望 `ApiResponse<T>`（`{code, message, data}` 格式）。

**修复**：`export_preview_handler.rs` 10 个 handler 改为 `Json<ApiResponse<Vec<T>>>` 用 `ApiResponse::ok(data)` 包装。

编译 ✅ 安装包已更新 `installer/工作量统计工具_Rust_v0.3.7_Setup.exe`

## v0.3.8 Bug 修复 — 仪器识别 + 实验室名称

### Bug 1: `extract_instrument` 返回"未知"
**文件**: `v0.3.8/src/api/export_data.rs` (第 109-123 行)
**修复**: 改进逻辑，使用 `rfind('[')` 找最后一个 `[`，然后在其后找第一个 `]`
**验证**: ✅ 所有调用位置传入 `m.full_name` 正确

### Bug 2: 实验室名称错误显示为"研发项目"
**根因**: `export_data.rs` 和 `stats_handler.rs` 的 SQL 查询 JOIN `project_groups pg` 时没有过滤"研发项目"伪分组
**修复**:
- `export_data.rs`: 6 处 Sheet 查询的 `LEFT JOIN project_groups pg` 加上 `AND pg.name != '研发项目'`
- `stats_handler.rs`: 重写所有统计 SQL，通过 `project_lab_links` 获取实验室名称（不再用 `p.group_id`）
**验证**: ✅ QA 严过关回归验证通过

### 前端 TypeScript 编译错误修复
**文件**: `project-root/frontend/src/types/index.ts`, `src/api/client.ts`, 多个页面组件
**修复**:
- `types/index.ts`: 添加缺少的属性（`project_count`, `group_name`, `coefficient`, `method_type`, `sample_count`, `unit`, `batch_no`, `submitted_at`, `project_id`, `SampleStats` 子类型）
- `api/client.ts`: 修复 `createRecord` 添加 `method_id?`, `createSample` 参数类型, 统计 API 添加 `group_by?`
- 页面组件: 修复 `undefined` 值处理

### v0.3.8 Bug 修复（第 4 轮 - 最终干净重建）
- **问题1**：用户录入记录实验室显示 `411,412` 数字（应为名称）
  - record_repo.rs SQL 子查询使用 `pg2.name` 正确，怀疑用户测试的是旧版本或数据问题
  - 需要用户安装最新包后验证
- **问题2**：导出功能依旧失败
  - 根因不明，已添加详细日志和前端错误处理
  - 前端 exportExcel 添加 HTTP 状态码检查（非200时解析错误JSON）
  - extract_instrument 添加 tracing debug/warn 日志
- **前端修复**：client.ts exportExcel 添加状态码检查 + 错误解析
- **操作**：
  - cargo clean --release 完全清理后重新编译
  - static 目录清理旧 JS 文件（之前有两个版本共存导致缓存）
  - Inno Setup 打包成功
- **用户反馈**：问题依旧存在，导出功能也坏了（需进一步排查）

## v0.3.8 Bug 修复（第二轮）— 录入页面 + 仪器识别规则 + method_id NULL 支持

### Bug 1: record_repo.rs 实验室显示"研发项目" — 已修复
- 修改 `list()` 和 `get_by_id_on_conn()` 的 SQL，通过 `project_lab_links` 获取实验室名称
- 过滤条件：`AND pg.name != '研发项目'`
- 编译 ✅

### Bug 2: extract_instrument 改用 @ 符号规则 — 已修复
- 新规则：`QL-260211-DAD@[LC-04]` → 提取 `LC-04`
- 兼容旧数据：如果没有 `@` 符号，仍尝试从 `[..]` 提取
- 文件：`src/api/export_data.rs` 第 108-135 行
- 编译 ✅

### Bug 3: method_id 为 NULL 的历史数据支持 — 已修复
- `models/record.rs`: `method_id: i64` → `Option<i64>`（RecordResponse 和 RecordCreate）
- `repo/record_repo.rs`: 所有 `row.get(2)?` 改为 `row.get::<_, Option<i64>>(2).unwrap_or(None)`
- `types/index.ts`: `WorkRecord` 添加 `method_id?: number`
- 编译 ✅ 前端构建 ✅

### 安装包（第二轮）
- `installer/工作量统计工具_Rust_v0.3.8_Setup.exe` ✅（重新打包）

## v0.3.9 阶段1 — 卡片式管理界面（就地编辑）

### 方案设计
- 文件：`project-root/frontend/docs/design/card-management-design.md`
- 三阶段：阶段1（就地编辑）→ 阶段2（拖拽关联）→ 阶段3（配置持久化）
- 版本隔离：v0.3.9 从 v0.3.8 复制源码+配置，不含 target/dist/installer

### 阶段1实施（就地编辑+关联可视化）
**新增文件**：
- `frontend/src/components/InlineEditCard.tsx` — 通用就地编辑卡片组件

**修改文件**：
- `frontend/src/pages/ManagePage.tsx`：
  - 移除项目/方法/实验室的编辑弹窗（Dialog），改为点击卡片展开/折叠编辑区
  - 新增 `expandedProjectId`/`expandedMethodId`/`expandedGroupId` 状态管理展开
  - 卡片上直接显示关联标签（蓝色圆角标签），点击 × 可取消关联
  - 实验室卡片点击后下方展开其下项目列表（只读）
  - 保留方法类型管理和导入映射对话框（仍用 Dialog）

**编译验证**：
- ✅ 前端 `npm run build` 通过（产物：`index-BN_J63ux.js` + `index-BpvLBu6K.css`）
- ✅ 后端 `cargo build --release` 通过（1个无关 warning：method_repo.rs unused `pid`）
- ✅ Inno Setup 打包成功：`installer/工作量统计工具_Rust_v0.3.9_Setup.exe`

**v0.3.9 目录结构**：
```
workload-tool-rust/v0.3.9/
  src/              — 从 v0.3.8 复制
  static/           — 前端构建产物
  Cargo.toml        — 从 v0.3.8 复制
  build.rs          — 从 v0.3.8 复制（需复制 icon.ico）
  build_installer.iss — 修改路径为 v0.3.9
  dist/             — release exe
  target/           — 编译缓存（不参与版本隔离）
```

**待实施（阶段2）**：
- 拖拽关联配置（dnd-kit）
- 左右分栏：左侧实验室卡片（拖拽源），右侧项目卡片（放置目标）
- 新增 `DraggableCard.tsx` + `DropZone.tsx`

