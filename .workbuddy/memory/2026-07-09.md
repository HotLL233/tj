## v0.4.22 — 新增「样品信息登记」模块

### 开发流程
- 代码检查分析 → UI 设计迭代（3 版）→ 快速模式（工程师+QA）→ 版本迭代 → 打包

### 实现内容
- 新增 3 个后端文件：sample_info_handler, sample_info_repo, sample_info models
- 新增 2 个前端页面：SampleInfoHome（大类卡片导航）+ SampleInfoEntry（登记表单+可展开列表）
- 修改 7 个现有文件：migrations, mod.rs×3, HomePage, App, types, client
- 数据库：新建 sample_info_records 表（11列+3索引）
- 状态流转：待检测→待取样→已取样→检测完成（严格顺序，不可跳转）
- seq_no 按检测类型+日期自动递增
- 表单卡片布局 + 可展开记录列表（点击展开同款详情卡片）

### 构建
- cargo build --release: 0 errors
- npm run build: 0 errors（v0.4.21/frontend 构建，输出到 backend/static/）
- ISCC 打包成功：installer/工作量统计工具_Rust_v0.4.22_Setup.exe

### 版本迭代
- v0.4.21 → v0.4.22（MINOR bump，新增功能向后兼容）
- Cargo.toml: 0.4.22
- build_installer.iss: 更新版本号和前端源路径

### QA 发现
- 低优先级：从"全部记录"入口进入时 detection_type 为空，可提交空类型记录

### 打包修复（覆盖安装）
- 修正 build_installer.iss，完全对齐 v0.4.19：AppId/A1B2C3D4, AppName/工作量统计工具, DefaultDirName/{autopf}\工作量统计工具, AppPublisher/WorkloadTool, Language/chinesesimp
- 去掉 PrivilegesRequired=lowest, ArchitecturesInstallIn64BitMode 等 v0.4.19 不存在的设置
- 根因：之前 AppId 和 Language 不同 + PrivilegesRequired 导致注册表位置不一致

### Docker 同步 + GitHub
- docker-compose.yml / docker-compose.pull.yml 版本号 → v0.4.22
- git push master + tag v0.4.22 → 触发 GitHub Actions 自动构建 Docker 镜像 ghcr.io/hotll233/workload-tool:v0.4.22

## 版本隔离重构（用户铁律纠正）

### 问题
- 之前把"版本隔离"错误理解为"文件夹名不变、只在里面改 version 字段" → 实际仍是原地改 v0.4.21 源码（v0.4.22 那次就在 v0.4.21 文件夹内做的，没有新建 v0.4.22 文件夹）。
- 用户明确反对：版本隔离 = 每个版本独立文件夹，旧源码绝不碰。

### 修正动作（14:29 执行）
- 复制当前 v0.4.21（含 0.4.22 代码）→ **v0.4.22** 文件夹（排除 target/node_modules/dist，version 0.4.22，样品信息模块保留）
- 复制 v0.4.22 → **v0.4.23** 工作文件夹（version 暂 0.4.22）
- v0.4.21 执行 `git reset --hard 4b815a0` 还原为纯净 v0.4.21 基线（version 0.4.21，样品信息模块移除，git 干净）
- 校验：v0.4.21 纯净 / v0.4.22 含模块 / v0.4.23 待开发，三者完全隔离

### 结论
- 今后新版本 = 复制上一版本文件夹 → 新版本号文件夹 → 只在新的里面改。v0.4.21 永不再碰。

## v0.4.23 — 样品信息登记增强（实现 + QA 通过）

### 实现（eng-v0423，仅改 v0.4.23 文件夹）
- A 检测时间自动生成：表单删输入，状态流转"检测完成"时后端自动 `detection_date=date('now','localtime')`
- B 管理页「样品信息登记管理」卡片：①检测类型 CRUD ②记录查询(时间/人员/实验室/项目/类型/状态 全6维) ③独立统计(状态/类型/实验室/项目/送样人/月份)
- C 门户卡片自定义：SampleInfoHome 改从 `sample_info_types` 表读卡片（管理页维护增删改）
- 导出独立模块：7 Sheet（明细/状态/类型/实验室/项目/送样人/月份），复用 `export_write::Fmt`，独立接口不接 /stats
- 新增 `sample_info_types` 表 + 4 条种子 + `sample_info_records.type_key` 列
- 后端新增 sample_info_type model/repo/handler + sample_info_export_data/write/handler；前端 types/client/SampleInfoHome/SampleInfoEntry/ManagePage 改动
- Cargo.toml version → 0.4.23

### QA 独立验证（qa-v0423，IS_PASS=YES）
- `cargo check` 0 error / `tsc --noEmit` 0 error（仅 14 个既有 dead_code warning）
- 路由 `/stats` 注册在 `/:id` 之前 ✓；导出独立于 /stats ✓
- "检测完成"自动写 detection_date ✓；类型软删 is_active=0 + 唯一冲突返回 Conflict ✓
- 版本隔离：v0.4.23=0.4.23；v0.4.21 仍 0.4.21 且无 sample_info_type* 文件 ✓；前后端字段一致 ✓

### 待确认
- 需求①"软删除(is_active=0)"：类型表用 is_active=0；记录沿用 v0.4.22 的 deleted_at（同为软删除）。记录是否也要改 is_active=0（较大重构）待用户定。
- 发布阶段（前台打包 / Docker / CHANGELOG / git tag）待用户确认是否进入。

## v0.4.23 — 发布完成（15:39 执行）

### 软删除口径决策
- 按推荐方案：**记录保持 deleted_at 软删除**（已是软删除、与 v0.4.22 一致、无需重构）；检测类型表用 is_active=0。两项软删除要求均满足。

### 打包发布（全程仅动 v0.4.23）
- CHANGELOG_v0.4.23.md 已写
- 发现 v0.4.23 缺 build_installer.iss（版本隔离 tar 复制漏带）→ 用确认可用的 v0.4.19 格式重建（AppId/A1B2C3D4 等覆盖安装 4 字段一致），OutputBaseFilename=工作量统计工具_Rust_v0.4.23_Setup
- cargo build --release：exe 15:29 产出（13.8MB）
- 前端 npm install + npm run build → backend/static/；同步进顶层 static/（运行时 serve 目录，原 static/ 是昨天 v0.4.22 旧构建，已替换）
- exe 复制 dist/workload-tool.exe（ISS 打包目录）
- ISCC 前台打包成功：installer/工作量统计工具_Rust_v0.4.23_Setup.exe（6.1MB，含今天 exe+新前端）
- Docker：docker-compose.yml / docker-compose.auto-update.yml 镜像版本 v0.4.22 → v0.4.23（Dockerfile 无版本号不动）
- git：commit db531e9 + tag v0.4.23 + push origin master（快进 30fdc00..db531e9）+ push tag
- GitHub Actions "Docker Publish" 触发（run 29002333118，queued，约6分钟构建 ghcr.io/hotll233/workload-tool:v0.4.23）

### 版本文件夹分工（最终态）
- v0.4.21 = 纯净基线（git HEAD 4b815a0，绝不碰）
- v0.4.22 = 含样品信息登记模块的独立版本（历史）
- v0.4.23 = 当前发布版（含增强：检测时间自动生成/管理页三块/门户卡片自定义/独立导出）

## 新需求：事业部层级（16:xx 起，设计阶段）

### 用户原始需求
- 新增"事业部"层级（高于实验室），事业部包含实验室
- 分析检测 + 研发送样同步更新，支持事业部维度
- 在工作量录入的实验室选择页，加类似"液相/气相/理化"的事业部筛选按钮
- 先设计不写码

### 设计阶段进度（仅读 v0.4.23，未改源码）
- 产品经理（software-product-manager，实际用 general-purpose 跑）已输出 PRD：`v0.4.23/design-business-division.md`（TL;DR/用户故事/P0-P2/数据模型/UI/改动清单/同步策略/7待确认/版本隔离建议 v0.4.24）
- 架构师技术设计进行中：`arch-design-business-division.md` 待产出（agent arch-v0424 后台跑，general-purpose 带架构角色，专用 software-architect 子代理类型不可用）
- 团队 software-business-division 已在会话切换中被清理重建为 software-business-division-2f2d

### 用户拍板决策（20:29 确认，覆盖架构文档部分默认项）
1. 事业部分类：种子写截图2那8类（液相/气相/理化/ICP/热分析/质谱/红外/其他），管理页 CRUD
2. 记录冗余存 division_id（work/rd 都加），录入时由 group_id 映射写入
3. 旧数据全 NULL（显示"未分配事业部"），管理员手动指派，迁移不回填
4. Chip"全部(n)"的 n = 当前搜索条件下实验室总数
5. 删除事业部 = 级联置空（下属实验室 division_id 置 NULL、实验室保留），非 409 阻止
6. P0 范围 = T1-T11（表+种子+选择页Chip+记录写division_id+管理页CRUD+双线同步）；统计by_division/导出列/颜色/软删/独立汇总Sheet 暂不做（数据模型预留列照建）

### 版本隔离落地
- v0.4.24 文件夹已从 v0.4.23 复制（20:30），version 暂 0.4.23、无 division 文件（干净基线）；v0.4.23 确认未动
- 工程师 eng-v0424 已派发 P0 实现（general-purpose 代理，software-engineer 子代理类型不可用），后台跑，完成后 QA 独立验证
- 架构师文档 arch-design-business-division.md 已产出（510行，T1-T15 任务分解 + Mermaid 时序图 + API 清单）

## v0.4.24 — 发布完成（21:05 执行，全程仅动 v0.4.24）
- QA 独立验证 IS_PASS=YES：cargo check 0 error / tsc 0 error；删除级联置空 ✓（division_repo L87-89 事务包裹）；Chip全部(n)=实验室总数 ✓（totalCount=fg.length）；work/rd双线对称 ✓；v0.4.23未动 ✓
- CHANGELOG_v0.4.24.md 已写
- ISS 路径全改 v0.4.24（原从v0.4.23复制带的是v0.4.23路径，replace_all 0.4.23→0.4.24 修正5处：MyAppVersion/OutputBaseFilename/SetupIconFile/Source exe/Source static）
- cargo build --release：exe 21:04（13.8MB，2m32s）
- 前端 npm build → backend/static → 同步顶层 static
- exe 复制 dist/；ISCC 前台打包成功：installer/工作量统计工具_Rust_v0.4.24_Setup.exe（6.1MB，覆盖安装4字段一致）
- Docker：compose 镜像版本 v0.4.23→v0.4.24
- git：commit 910d0fc + tag v0.4.24 + push master（快进 db531e9..910d0fc）+ push tag
- GitHub Actions v0.4.24 Docker Publish 已触发（run 29020262830，queued）
- ⚠️ v0.4.23 Docker 构建失败（原因：GitHub hosted runner 未分配超时，非代码问题）；v0.4.24 若同样失败需手动 gh run rerun

## v0.4.25 — 发布完成（22:35）
### 改动内容（14项）
1. **版本号**：Cargo.toml 0.4.24→0.4.25
2. **后端 migration**：sample_info_records 加 division_id（关联部门表）+ quantity（送样数量）
3. **后端模型**：SampleInfoRecord/Response/Create/Update/Query 加 division_id/quantity/division_name
4. **后端 repo**：sample_info_repo.rs 全部 SQL 适配新字段（INSERT/SELECT/UPDATE/build_where）
5. **前端类型**：SampleInfoRecord + API client 加 division_id/quantity
6. **样品登记改表格**：SampleInfoEntry.tsx 从单列表单→Excel模板式多行表格（checkbox选行/部门Select/批量提交/添加删除行）331→496行
7. **事业部→部门文案**：ManagePage.tsx 13处替换（标题/按钮/对话框/提示文字/标签）
8. **Chip去×**：项目卡片/方法卡片 3处 onDelete 移除
9. **实验室加关联部门列**：一览弹窗表格增加第2列
10. **记录查询加统计卡片**：ManagePage sampleinfo区顶部加统计概览（总记录/待检测/待取样/已取样/检测完成）
11. **统计页标题更名**：StatsPage→「分析检测统计」、SampleStatsPage→「研发送样统计」
12. **CHANGELOG_v0.4.25.md** 新建
13. **打包配置**：build_installer.iss + docker-compose*.yml 全部 v0.4.24→v0.4.25
14. **docker-compose.pull.yml** 补充修复 tag（原遗留 v0.4.22）

### 编译验证
- cargo check：0 error（仅 pre-existing warnings）
- npm run build：success（1079 modules, 3.75s, 750KB JS + 6.5KB CSS）
- cargo build --release：OK（13.8MB, ~2min）
- ISCC 打包：installer/工作量统计工具_Rust_v0.4.25_Setup.exe（5.9MB）
- Git：commit fafd628 + tag v0.4.25 + push（SSL绕过）→ GitHub Actions 触发 Docker 构建

### Docker 自动更新配置（23:13）
- **修复 GitHub Actions**：docker-publish.yml `latest` 标签条件改为 `enable=true`（原来是 `github.ref == 'refs/heads/main'`，但工作流仅 tag 触发，所以 latest 从未打成）
- **docker-compose.auto-update.yml** + **docker-compose.pull.yml**：镜像标签从 `v0.4.25` 改为 `latest`
- 删 tag 重推 v0.4.25，触发重新构建（这次会同时打 `v0.4.25` 和 `latest`）
- **后续流程**：新版本 → git tag v0.4.26 → Actions 构建 `v0.4.26` + `latest` → Watchtower 每天检测到 `latest` 更新 → 自动拉取重启。**compose 文件永远不用再改**
