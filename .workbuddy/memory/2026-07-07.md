# 2026-07-07 工作日志

## v0.4.5 交付（11:10 完成）
- BugFix: rd_record_count 子查询从 project_lab_links 改为 wr.group_id（3处SQL）
- 额外修复: rd_stats_handler.rs(3处) + rd_export_data.rs(1处) 同样绕路问题
- 前端: 方法行系数标签改为单价，取样人行内编辑(blur保存)，统计入口移到标题右侧
- 打包: installer/工作量统计工具_Rust_v0.4.5_Setup.exe (5.3MB)

## v0.4.6 进行中（17:00 开始）
- 方案设计完成(5张mockup)，用户确认所有需求
- 倍率: methods + work_records 加 multiplier 字段，金额=数量×单价×倍率
- 仪器色: #9c27b0 → #00897b（6处）
- 筛选修复: stats页筛选改用后端全量人名列表
- nowrap修复: stats页数据列加 whiteSpace:nowrap
- 取样人行内编辑: SampleEntryPage + RdRecordsPage
- 统计入口: SamplePortal 标题右侧
- 版本隔离: cp v0.4.5 → v0.4.6，工程师并行开发中

### 4 项修复（用户发现的问题）
1. **新建项目 FK 错误**：`project_repo::create` 硬编码 `group_id=0` + `method_type='研发项目'` → 改为取 `lab_ids[0]` 作为 group_id，INSERT 写入 full_name/sort_order/is_active，移除 method_type 硬编码。`ProjectCreate` 模型同步扩展。
2. **全模块「人员/用户/用户名」→ 检测人/送样人**：录入页（EntryPage/SampleEntryPage）+ 统计页（StatsPage/SampleStatsPage）内所有可见中文标签、校验提示、列头、筛选下拉、Sheet 说明统一改为模块对应称呼。共 4 文件 30+ 处编辑。
3. **卡片颜色按模块统一**：`GroupCard.tsx`（门户实验室卡片）+ `StatsCards.tsx`（统计汇总卡片）新增 `themeColor` prop。分析检测 `#1976d2`（蓝），研发送样 `#e65100`（橙）。WorkloadPortal/SamplePortal/StatsPage/SampleStatsPage 分别传对应色。原彩虹/六色行为保留为 fallback。
4. **研发送样返回标题回退**：`SampleEntryPage` 返回按钮 `navigate('/workload')` → `navigate('/sample')`。

### 验证
- cargo check：零错误
- tsc --noEmit：零错误
- 构建：npm run build + cargo build --release 通过
- ISCC：`installer/工作量统计工具_Rust_v0.4.2_Setup.exe`（~5.5MB），覆盖安装（AppId 不变）

### 版本隔离
- `cp -r v0.4.1 v0.4.2`（后端+前端源码独立）
- Cargo.toml/buid_installer.iss → 0.4.2
- UPDATE_NOTES.md 已重写

### 文件改动
- 后端：`models/project.rs`、`repo/project_repo.rs`
- 前端：`EntryPage.tsx`、`StatsPage.tsx`、`SampleEntryPage.tsx`、`SampleStatsPage.tsx`、`GroupCard.tsx`、`StatsCards.tsx`、`WorkloadPortal.tsx`、`SamplePortal.tsx`
- 打包：`Cargo.toml`、`build_installer.iss`、`UPDATE_NOTES.md`、frontend `static/`

---

## v0.4.3 研发送样状态+取样人（10:00 完成）

### 新增功能
1. rd_work_records 新增 status/sampler/sampled_at 三列（migration）
2. 新建 RdRecordResponse 模型（独立于共享 RecordResponse，避免影响 work_records）
3. SampleEntryPage 表格 8→12 列：序号/状态/录入时间/实验室/项目/送样人/方法/类型/仪器/数量/取样人/取样时间
4. 状态列：待取样黄底(#fff9c4)/已取样绿底(#c8e6c9)
5. 取样人列可点击→弹窗输入→自动记录时间+状态变已取样
6. 实验室卡片（GroupCard）加「X 条送样」计数，两模块门户均可见

### 验证
- cargo check：零错误
- tsc --noEmit：零错误
- 构建：npm build + cargo build --release 通过
- ISCC：`installer/工作量统计工具_Rust_v0.4.3_Setup.exe`（~5.5MB）

### 关键决策
- 使用独立 RdRecordResponse 模型，不修改共享 RecordResponse，确保 work_records 完全不受影响
- 取样人列统一调 sampleRdRecord API（已有记录的取样人修改也只调此 API，status=已取样→已取样是无操作）

### 文件改动
- 后端：migrations.rs、models/rd_record.rs(新)、models/mod.rs、models/group.rs、repo/rd_record_repo.rs、repo/group_repo.rs、api/rd_record_handler.rs、service/rd_record_service.rs
- 前端：types/index.ts、api/client.ts、components/GroupCard.tsx、pages/SampleEntryPage.tsx
- 打包：Cargo.toml、build_installer.iss、UPDATE_NOTES.md

---

## v0.4.4 研发送样记录独立卡片+角标+送样时间改名（10:50 完成）

### 新增
1. RecordsCard 组件：与实验室卡片同级，两模块门户均显示，右上角角标显示全局待取样数
2. RdRecordsPage：全量研发送样记录页（12列表格，取样人可编辑弹窗）
3. 实验室卡片 GroupCard：移除底部送样 Chip，改为右上角圆形角标（橙色，#f57f17）
4. group_repo rd_record_count 语义改为「待取样数」（SQL 加 AND status='待取样'）

### 修改
- SampleEntryPage 表头「录入时间」→「送样时间」
- SamplePortal 手机端 grid xs: '1fr' → 'repeat(3,1fr)'（与分析检测一致）

### 验证
- cargo check/tSC/build/ISCC 全通过
- 安装包：installer/工作量统计工具_Rust_v0.4.4_Setup.exe

### 文件改动
- 后端：repo/group_repo.rs
- 前端：components/GroupCard.tsx、components/RecordsCard.tsx(新)、pages/SamplePortal.tsx、pages/WorkloadPortal.tsx、pages/SampleEntryPage.tsx、pages/RdRecordsPage.tsx(新)、App.tsx
- 打包：Cargo.toml、build_installer.iss、UPDATE_NOTES.md
