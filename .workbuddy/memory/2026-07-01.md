# 2026-07-01

## v0.2.17 BugFix — 5项修复 (09:29)

- 方法导入类型匹配: classify_method_type 返回实际类型名 (液相/气相/理化等) 而非 "检测方法"
- 审计日志补全: method_repo create/update/delete 增加 audit_repo::log 调用
- 回收站修复: 过滤条件 !x.is_active → x.deleted_at != null
- 实验室筛选清理: 研发项目管理卡片删除 lab 下拉 + 批量系数
- 备份统计: tables 数组新增 "methods"

### 需求
三卡片（研发项目/方法/实验室）完全独立，方法从 projects 表拆分到 methods 表。

### 版本隔离
从 v0.2.16 复制 → v0.2.17，Cargo.toml 版本号已更新。

### 新建文件 (3)
- `src/models/method.rs` — MethodResponse/Create/Update
- `src/repo/method_repo.rs` — methods CRUD + method_types
- `src/api/method_handler.rs` — Router: /api/methods, /api/methods/import

### 修改文件 (11)
- `db/migrations.rs` — 新建 methods 表 + 3张关联表 + 数据迁移
- `models/project.rs` — 精简 ProjectResponse/Create/Update
- `models/mod.rs` — 添加 method 模块
- `repo/project_repo.rs` — 重写 CRUD (JOIN 关联表)
- `repo/mod.rs` — 添加 method_repo
- `api/project_handler.rs` — 移除 method_import 等
- `api/mod.rs` — 添加 method_handler 路由
- `seed.rs` — 适配新表
- 前端: types/index.ts, api/client.ts, ManagePage.tsx

### QA 验证
- cargo check: ✅ 0 errors
- 7 项审查: ✅ 全部 PASS

### 关键设计
- methods 表完全独立，不与 projects 混合
- project_lab_links / project_method_links / method_type_links 关联表
- work_records 不变，仍通过 project_id 关联
- 导入路由从 /api/projects/method-import → /api/methods/import
## 2026-07-01 — v0.2.19 项目编辑/方法编辑优化

### 完成内容
**快速模式交付：工程师 + QA 验证全部通过**
- 项目编辑对话框：恢复全称(full_name)/排序(sort_order)/启用(is_active)，移除管理系数
- 方法编辑对话框：增加金额(amount)字段+方法类型复选框组
- 方法管理卡片：增加类型筛选下拉框(液相/气相/理化/ICP/热分析/质谱/其他)
- 数据库迁移：methods 表 ALTER TABLE ADD amount REAL
- 实验室删除Bug修复：project_count 改用 project_lab_links 关联

### 文件改动 (12个)
后端 v0.2.19/: migrations.rs, models/method.rs, models/project.rs, repo/method_repo.rs, repo/project_repo.rs, repo/group_repo.rs, Cargo.toml, build_installer.iss, DESIGN_PLAN.md
前端: types/index.ts, api/client.ts, pages/ManagePage.tsx

### QA 结果
第1轮: 发现 updateProject 类型缺少3字段 → 工程师修复
第2轮: 回归全部通过 ✅

### 预存问题 (非v0.2.19范围)
GroupCard.tsx, ProjectRow.tsx, EntryPage.tsx, Sample系列页面 多处类型缺失

### 编译打包 (15:43)
cargo build --release ✅ (10.26s) | vite build ✅ (2.53s, 跳过tsc预存错误) | Inno Setup ✅ (3.1s)
安装包: installer/工作量统计工具_Rust_v0.2.19_Setup.exe

## v0.3.0 — 数据导入系统灵活化 (16:03)

### 核心改动
- import_mappings 表驱动列头路由（替代 5行硬编码 if-else）
- 11条种子 wildcard 映射（*实验室*→group, *项目*→project, *液相*→methods等）
- 项目-实验室同行 M:N 自动关联（同行笛卡尔积 → project_lab_links）
- 前端方法级联选择（类型 Select → 过滤 Autocomplete）
- 前端导入映射预览对话框

### 文件改动 (10个)
后端: models/import.rs(新), models/mod.rs, db/migrations.rs, repo/method_repo.rs, api/method_handler.rs(重写), Cargo.toml, build_installer.iss
前端: types/index.ts, api/client.ts, pages/ManagePage.tsx

### QA 结果
第1轮: 发现2个Bug — method_repo.rs类型注解 + ManagePage.tsx重复Autocomplete → 工程师修复
第2轮: cargo check ✅ vite build ✅ 全部通过

### 版本隔离
v0.2.19 → v0.3.0，Cargo.toml/buid_installer.iss 版本号已更新

### 编译打包 (16:30)
vite build ✅ (2.51s) | cargo build --release ✅ (43.89s) | Inno Setup ✅ (3.0s)
安装包: installer/工作量统计工具_Rust_v0.3.0_Setup.exe

