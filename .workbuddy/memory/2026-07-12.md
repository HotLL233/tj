## v0.4.40 (15:30-15:35)
- **真正修复导出**: stats_service::by_division SQL 引用不存在的 pg2.deleted_at → 改成直接查 pg2.division_id = d.id
- 加 migration ALTER TABLE project_groups ADD COLUMN deleted_at TEXT（向后兼容）
- 改动仅 2 个 Rust 文件，前端零改动
- tj + workload-tool 双仓库推送成功

## v0.4.39 (13:00-13:35)
- Axios responseType: 'blob' bug → 改用 fetch + 手动 token
- 导出错误消息透传到前端
